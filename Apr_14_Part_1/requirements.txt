1) Admin Detais

-> Add Admin
-> Login Admin

Once Login Admin Happens Define dashboard (admin) as

Add Doctor  AddAppointment UpdateAppointment CancelAppointment

AddPatient AdmitPatient

PatientHistory : Show All info about his medical history 

Add Medical History : 

Doctors : DoctorsShow DoctorSearch DoctorLogin

hibernate.cfg.xml : It is a configuration file that which contains all information about database connections. 

The dialect property  is org.hibernate.dialect.MySQLDialect which tells the Hibernate that we are using MySQL Database. Hibernate supports many database. With the use of the Hibernate (Object/Relational Mapping and Transparent Object Persistence for Java and SQL Databases),  we can use the following databases dialect type property:

If you want to convert Hibernate code into database understandable format, then we need the help of dialect.

How we need to implement the mapping.

Assume, we have Employ class which contains fields. And for hibernate table must have primary key. 

public class Employ
{
   private int empno;
   private String name
}

Now this class i want to map to database, then 

we need to use annotation as 
@Entity : Ensures this class is database class. will be
used for mapping.

@Table : Ensures for which table it has to be mapped further.

@Id : Its a primary key. 

Now, Employ.java class to be converted as POJO class, as 

Plain Old Java Object 

import lombok.*;

@AllArgsConstructor
@NoArgsConstructor
@Getter
@Setter
@ToString
@Entity
@Table(name="Employ")
public class Employ {

  @Id 
  private int empno;
  private String name;
  private Gender gender;
  private String dept;
  private String desig;
  private double basic;

}


<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
  "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
  "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
  <session-factory>
    <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
    <property name="hibernate.connection.password">root</property>
    <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/infinite26</property>
    <property name="hibernate.connection.username">root</property>
    <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>
    <property name="show_sql">true</property>
  </session-factory>
</hibernate-configuration>


hibernate.cfg.xml file contains all the information about db connections and mappings.

    <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property> : Ensures connecting to MySQL

    <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property> : Ensures to convert Hibernate code into Sql Format. 

Hibernate supports HQL (hibernate query language) as dialect converts HQL into SQL understandable format. 

