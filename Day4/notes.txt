1) Collections : Arrays are static data structures, means once assigned we cannot alter the size. But, if you want to add the elements irrespective of size, then we need the help of collections. 

Collections can grow to any size unlike arrays. 

Collections are from java.util package.

Collections stores data in Object format. 

The top 3 interfaces of collection framework are 

(i) List : Allows you to store list of values. 

ArrayList, Vector, LinkedList are falling under List interface

(ii) Set : Allows you to eliminate duplicate entries from the given collection. HashSet and LinkedHashSet are falling under set Interface

HashSet displays records after removing duplicates in Hashing order, where LinkedHashSet used to display records in Insertion Order. 

(iii) SortedSet : Allows you to display records in Sorted Order. TreeSet is falling under SortedSet interface. 

"C:\Program Files\Java\jdk-new-17\bin\java.exe" "-javaagent:C:\Program Files\JetBrains\IntelliJ IDEA 2025.3\lib\idea_rt.jar=59400" -Dfile.encoding=UTF-8 -classpath D:\infinite\Day4\DemoProject\out\production\DemoProject com.java.day4.SortEmploy
Exception in thread "main" java.lang.ClassCastException: class com.java.day4.Employ cannot be cast to class java.lang.Comparable (com.java.day4.Employ is in unnamed module of loader 'app'; java.lang.Comparable is in module java.base of loader 'bootstrap')
	at java.base/java.util.TreeMap.compare(TreeMap.java:1569)
	at java.base/java.util.TreeMap.addEntryToEmptyMap(TreeMap.java:776)
	at java.base/java.util.TreeMap.put(TreeMap.java:785)
	at java.base/java.util.TreeMap.put(TreeMap.java:534)
	at java.base/java.util.TreeSet.add(TreeSet.java:255)
	at com.java.day4.SortEmploy.main(SortEmploy.java:10)

Process finished with exit code 1

Map Interface : Allows you to store data w.r.t. Key/Value Pairs. Here, the key cannot be duplicated or null, but value can be repeated. 

HashTable and HashMap are falling under Map interface. 

Generics : Provides an opportunity to protect data. If you want to define data type at runtime then we need the help of generics. 

Generics will be represented using <T>, as in place <T>, we can pass any value type/ref type or custom type. 

Generic methods ensures write once and apply any data type. 

Advantages :
___________


1) Write once and apply for all types

2) Provides security to the data

3) No Typecasting required

4) Reduces the size of the program. 

void show(int x) {
...
...
}

show();
show(12);
show(12,5);

varargs(...) : If any method contain varargs(...) then we can pass to methods from zero to n no.of args. 

void show(int...x) {
...
...
}

then show() all above pars will work.

StringBuilder() : It is a mutable class, that which allows you to perform changes in the given string. 

Exception Handling : It is a mechanism of handling the runtime errors during the execution of the program. 

There are 2 types of errors known as 

(i) Compile Time

(ii) Runtime 

To handle exceptions, we have 3 blocks known as 

(i) try block : This block contains statements in which there is possibility of getting runtime errors, the statements which are placed in this block are treated as heavy weighted statement(s) as JVM 
allocates excess memory to them.

(ii) catch block : Once exceptions raised in try block, then the control will be forwarded to catch block, this block contains alternate statements for the exceptions raised in try block. 

One try block can contains multiple catch blocks. 

(iii) finally block : This block contains statements that needs to be executed mandatorily irrespective of try and catch blocks, this block is not mandatory, but all the statements which are placed in this block are executed as mandatory statements. 

Exception is the top most class, from which all kinds of exceptions are derived. 

syntax : 
_________

try {
...
...
} catch (Exception e) {
...
...
} finally {
...
...
}

There are 2 types of Exceptions known as 

(i) Checked Exceptions : These are the exceptions that which occurs at compile time, during compilation JVM feels some erroneous code is there and stops the execution  at compile time, can be handled at method level by using throws keyword.

(ii) Unchecked Exceptions : These are the exceptions that which occurs at runtime can be handled by using try...catch block. 

throw keyword : If you want to raise any error object with message, then we need the help of throw keyword. 



















